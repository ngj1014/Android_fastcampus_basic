package com.example.myapplication.안드로이드_UI

//안드로이드에서 화면을 그리는 방법
//XML을 이용한다.
// -XML은 뭘까?
// -DSL Language -> Domain Specific Language
// -안드로이드 UI를 그리기 위해 특화된 언어이다.


// 핸드폰 마다 화면 크기가 다 다른데 어떻게 화면을 그려야 할까?
// -픽셀 ,dpi, dp단위
// -픽셀 ->핸드폰 화면에서 빛이 나오는 전구 ->가장작은 단위
// -dpi ->dot per inch
//               -ldpi ->120(1인치에 120 픽셀)
//               -mdpi ->160(1인치에 160 픽셀)
//               -hdpi ->240(1인치에 240 픽셀)
//               -xhdpi ->320(1인치에 320 픽셀)
//               -xxhdpi ->480(1인치에 480 픽셀)
//               -xxxhpi ->640(1인치에 640 픽셀)
// -dp ->Density Independent Pixcel
//        -픽셀 독립적인 단위(픽셀이아니라 dp로 그려야지 똑같은 그림이 그려진다. )

//부모는 화면에 나오지않고 배치가 어떻게 될지.. 설정